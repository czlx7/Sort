#include<stdio.h>
#include<stdlib.h>
// typedef int a;

int main() {
    // a b = 1;
    // //指向地址为0位置的子例程（函数）
    // (*(void(*)())0)();
    int a = 1/2;
    printf("%d\n",a);
    system("pause");
    return 0;
}